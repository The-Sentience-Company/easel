---
name: Bug report
about: Something easel does wrong
labels: bug
---

**What happened, and what you expected instead**

**Steps to reproduce** — the `easel` command you ran, and the template/data if relevant.

**Environment** — macOS version, `node --version`, `easel status` output.

**Daemon log** — the relevant lines from `~/.easel/daemon.log`.
